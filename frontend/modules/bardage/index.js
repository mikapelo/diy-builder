/**
 * index.js — Interface publique du module Bardage
 *
 * Point d'entrée unique : expose le moteur de calcul et la config.
 */
export { generateBardage } from './engine.js';
export { bardageConfig } from './config.js';
